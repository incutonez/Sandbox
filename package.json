{
  "name": "@incutonez/sandbox",
  "version": "1.0.0",
  "type": "module",
  "private": false,
  "scripts": {
    "ui": "cd PrimeVue && npm run dev",
    "api": "cd API && npm run start:dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "updateDeps": "node ./updateDependencies.js",
    "lint": "npx eslint . --ext .js --fix",
    "lint:fix": "prettier --write && npx eslint --fix",
    "explode": "npm cache clean --force && npx rimraf package-lock.json node_modules && npm i",
    "build:apiSpec": "cd APISpec && npm run build",
    "build:ui": "cd PrimeVue && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/incutonez/Sandbox.git"
  },
  "bugs": {
    "url": "https://github.com/incutonez/applications/issues"
  },
  "homepage": "https://github.com/incutonez/Sandbox#readme",
  "devDependencies": {
    "@semantic-release/git": "^10.0.1",
    "eslint": "^8.17.0",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-simple-import-sort": "^10.0.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "multi-semantic-release": "^3.0.2"
  },
  "workspaces": [
    "Differ/api",
    "Differ/shared",
    "Differ/ui",
    "TipTap",
    "Modeling",
    "PrimeVue",
    "API",
    "APISpec"
  ],
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  },
  "lint-staged": {
    "*.{js,mjs,cjs,jsx,ts,tsx,vue}": [
      "eslint --fix"
    ]
  },
  "release": {
    "branches": [
      "main"
    ],
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      [
        "@semantic-release/npm",
        {
          "npmPublish": false
        }
      ],
      "@semantic-release/git",
      "@semantic-release/github"
    ]
  }
}
